name: family_finance_mobile
description: Aplicacion Flutter para gestionar las finanzas familiares conectada al backend existente.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^10.1.9
  http: ^1.2.2
  shared_preferences: ^2.3.2
  connectivity_plus: ^6.1.0
  sqflite: ^2.3.3+1
  path: ^1.9.0
  path_provider: ^2.1.5
  share_plus: ^10.1.2
  speech_to_text: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
